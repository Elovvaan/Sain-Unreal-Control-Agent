{
  "name": "sane-unreal-control-agent",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "local:unreal": "cross-env HOST=127.0.0.1 PORT=3000 UNREAL_BRIDGE_URL=${UNREAL_BRIDGE_URL:-http://127.0.0.1:30010} python server.py"
  },
  "devDependencies": {
    "cross-env": "^7.0.3"
  }
}
